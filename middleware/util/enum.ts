export enum STATUS_MESSAGE {
  SUCCESS = "success",
  FAIL = "fail",
  ERROR = "error",
}

export enum STATUS_CODES {
  NOT_FOUND = 404,
  INTERNAL_SERVER_ERROR = 500,
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
}
